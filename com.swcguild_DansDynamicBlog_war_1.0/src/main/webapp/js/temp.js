$(document).ready(function () {
    $('#blog_entry').click(function (event) {
        event.preventDefault();
// Make an Ajax call to the server. HTTP verb = POST, URL = contact 
        $.ajax({
        }).success(function (data, status) {

        });
    });

    $('#').click(function (event) {
        event.preventDefault();
        
    });
});